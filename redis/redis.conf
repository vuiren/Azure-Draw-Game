# Bind to localhost and internal private IP for security
bind 127.0.0.1 10.0.0.5

# Default listening port
port 6379

# Protected mode ensures security if bind is missing
protected-mode yes

# Close client connections after being idle (in seconds; 0 disables it)
timeout 0

# TCP keepalive to detect dead connections
tcp-keepalive 300